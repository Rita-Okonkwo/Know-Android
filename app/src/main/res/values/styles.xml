<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:textColorSecondary">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--MDC Contained Button Theme. -->
    <style name="Widget.Know.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">#FEFEE3</item>
        <item name="backgroundTint">#2C6E49</item>
        <item name="android:stateListAnimator" tools:ignore="NewApi">
            @animator/button_anim
        </item>
    </style>

    <!--Toolbar Theme. -->
    <style name="Widget.Know.Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="android:theme">@style/AppTheme</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>

    <!--Hint text appearance theme. -->
    <style name="TextAppearance.Know.TextInputLayout.HintText" parent="TextAppearance.MaterialComponents.Subtitle2">
        <item name="android:textColor">#444053</item>
        <item name="android:textSize">12sp</item>
        <item name="android:fontFamily">@font/raleway</item>
    </style>

    <!--Input text appearance theme. -->
    <style name="TextAppearance.Know.TextInputLayout.InputText" parent="TextAppearance.MaterialComponents.Subtitle1">
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/roboto</item>
    </style>

    <!-- Edit text box theme. -->
    <style name="Widget.Know.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="hintTextAppearance">@style/TextAppearance.Know.TextInputLayout.HintText</item>
        <item name="hintTextColor">#444053</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="boxStrokeColor">#444053</item>
    </style>

</resources>
